/**
 * 禁止选中
 */
// document.onselectstart = function (event) {

//     if (window.event) {
//         event = window.event;
//     }

//     try {
//         var the = event.srcElement;
//         if (!(("INPUT" === the.tagName && "text" === the.type.toLowerCase()) || "TEXTAREA" === the.tagName)) {
//             return false;
//         }
//         return true;
//     } catch (e) {
//         return false;
//     }

// }
